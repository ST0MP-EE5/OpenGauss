# Benchmark Context

This is a benchmark-only native OpenGauss run on FormalQualBench.

Priorities:
1. produce a Comparator-valid `Solution.lean`
2. keep the proof path direct and reproducible
3. avoid expanding scope into unrelated local library work unless it is strictly necessary

Treat theorem-local hints as guidance, not as claims of correctness.
